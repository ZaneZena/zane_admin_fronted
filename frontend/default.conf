server {
    listen       80;
    server_name  localhost;

    location / {
        # 容器内的静态文件目录
        root   /usr/share/nginx/html;
        index  index.html index.htm;
        
        # 🌟 核心配置：解决 Vue 路由 History 模式刷新 404 的问题
        try_files $uri $uri/ /index.html;
    }

    # 错误页面配置
    error_page   500 502 503 504  /50x.html;
    location = /50x.html {
        root   /usr/share/nginx/html;
    }
}